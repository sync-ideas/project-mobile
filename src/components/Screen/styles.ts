import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		// paddingHorizontal: 16,
		backgroundColor: '#fff',
		paddingVertical: 10,
	},
	containerSecondary: {
		width: '100%',
		height: '100%',
	},
})
